name: E2E - Chrome Headless (Playwright, Cypress, Selenium)

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"

      - name: Install root dependencies
        run: npm ci

      - name: Prepare Allure results dir
        run: |
          rm -rf allure-results
          mkdir -p allure-results

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Run Cypress tests (Chrome headless)
        run: npx cypress run --browser chrome --env allure=true,allureClearResults=false

      - name: Debug Allure after Cypress
        if: always()
        run: |
          echo "Allure results after Cypress:"
          ls -la allure-results || true
          echo "Count:"
          find allure-results -maxdepth 1 -type f | wc -l || true

      - name: Run Playwright tests (Chrome headless)
        run: npm run ci:playwright:chrome

      - name: Debug Allure after Playwright
        if: always()
        run: |
          echo "Allure results after Playwright:"
          ls -la allure-results || true
          echo "Count:"
          find allure-results -maxdepth 1 -type f | wc -l || true

      - name: Install Selenium dependencies
        run: npm ci
        working-directory: selenium

      - name: Setup Chrome
        uses: browser-actions/setup-chrome@v1

      - name: Setup Chromedriver
        uses: nanasess/setup-chromedriver@v2

      - name: Show Chrome & Chromedriver versions
        run: |
          google-chrome --version || true
          chromedriver --version || true
          which google-chrome || true
          which chromedriver || true

      - name: Export CHROMEDRIVER_PATH
        run: echo "CHROMEDRIVER_PATH=$(which chromedriver)" >> $GITHUB_ENV

      - name: Diagnose chromedriver precedence
        run: |
          echo "CHROMEDRIVER_PATH=$CHROMEDRIVER_PATH"
          echo "PATH=$PATH"
          ls -la selenium/node_modules/.bin | head -n 50 || true
          if [ -f "selenium/node_modules/.bin/chromedriver" ]; then
            echo "Local selenium/.bin chromedriver version:"
            selenium/node_modules/.bin/chromedriver --version || true
          else
            echo "No local selenium/.bin chromedriver"
          fi

      - name: Run Selenium tests (Chrome headless)
        env:
          CHROMEDRIVER_PATH: ${{ env.CHROMEDRIVER_PATH }}
        run: npm run ci:selenium:chrome:headless

      - name: Debug Allure after Selenium
        if: always()
        run: |
          echo "Allure results after Selenium:"
          ls -la allure-results || true
          echo "Count:"
          find allure-results -maxdepth 1 -type f | wc -l || true

      - name: Generate Allure report
        if: always()
        run: npm run allure:generate

      - name: Upload Allure report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: allure-report
          if-no-files-found: ignore

      - name: Upload Allure results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results
          path: allure-results
          if-no-files-found: ignore

      - name: Upload test artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-artifacts
          path: |
            playwright-report
            test-results
            blob-report
            cypress/screenshots
            cypress/videos
            selenium/artifacts
          if-no-files-found: ignore
